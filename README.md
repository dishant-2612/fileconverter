convert .doc, .docx, .xls. xlsx, .txt, .htm, .html, .ppt, .pptx to .pdf using java spring boot maven project.

docker-compose up --build to run the application locally.

to test in browser: http://localhost:8080/
